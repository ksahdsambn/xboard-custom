# TASK-019 isolated runtime

Installs MobileApp on a frozen official Xboard copy and verifies envelope, P0 error mapping, session 401/403 vs business 403, locale-stable machine codes, request IDs and log redaction.

Not a production plugin. `formalAcceptanceClaimed` is always false.
